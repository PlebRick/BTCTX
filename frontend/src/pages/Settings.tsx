// src/pages/Settings.tsx
import React from 'react';

const Settings: React.FC = () => {
  return (
    <div>
      <h2>Settings</h2>
      <button className="btn">Log Out</button>
      {/* Future logic for user settings or preferences */}
    </div>
  );
};

export default Settings;